[package]
name = "lingua-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
lingua = { version = "1.8", default-features = false, features = ["danish", "english", "polish"] }
wasm-bindgen = "0.2"
